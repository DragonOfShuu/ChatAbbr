import './Root.sass'

const Root = () => {
    return (
        <div className={`pd-size-full pd-flex pd-place-items-center pd-place-content-center`}>
            <h1 className={`pd-text-9xl`}>
                {`INJECTION SEQUENCE`}
            </h1>
        </div>
    );
}

export default Root;